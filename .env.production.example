# =============================================================================
# Choose the Heat - Production Environment Configuration (Vercel + Neon)
# =============================================================================
# Use this as reference when setting up environment variables in Vercel
# DO NOT commit actual values to Git
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get from: Neon dashboard → Connection Details → Pooled connection
# IMPORTANT: Must use POOLED connection for Vercel serverless
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# -----------------------------------------------------------------------------
# Application Settings (REQUIRED)
# -----------------------------------------------------------------------------
# Your Vercel deployment URL (update after first deploy)
APP_URL=https://your-app.vercel.app

# Production mode
NODE_ENV=production

# -----------------------------------------------------------------------------
# Session Secret (REQUIRED)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
# CRITICAL: Use a different secret for production than development
SESSION_SECRET=your-production-secret-here-CHANGE-ME

# -----------------------------------------------------------------------------
# Site Password Protection (RECOMMENDED for staging/testing)
# -----------------------------------------------------------------------------
# Protect the entire site with HTTP Basic Auth
# Recommended for staging deployments to prevent public access
# Users will see a browser password prompt before accessing the site
SITE_PASSWORD=your-secure-password-for-testers

# -----------------------------------------------------------------------------
# AI Provider Configuration (REQUIRED - Choose ONE)
# -----------------------------------------------------------------------------
# Which provider to use: openai, anthropic, google, or mistral
AI_PROVIDER=openai

# OpenAI (Recommended for production)
OPENAI_API_KEY=sk-proj-...your-production-key
OPENAI_MODEL=gpt-4-turbo

# OR Anthropic Claude
# ANTHROPIC_API_KEY=sk-ant-...your-key
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# OR Google Gemini
# GOOGLE_GENERATIVE_AI_API_KEY=...your-key
# GOOGLE_MODEL=gemini-1.5-pro

# OR Mistral
# MISTRAL_API_KEY=...your-key
# MISTRAL_MODEL=mistral-large-latest

# -----------------------------------------------------------------------------
# Google OAuth (OPTIONAL)
# -----------------------------------------------------------------------------
# Only needed if you want Google social login
# Redirect URI must be: https://your-app.vercel.app/api/auth/callback/google
GOOGLE_CLIENT_ID=...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=...your-secret

# -----------------------------------------------------------------------------
# Redis Configuration (OPTIONAL - Not currently used)
# -----------------------------------------------------------------------------
# If you implement Redis caching later, use Upstash Redis
# Get from: upstash.com → Redis → Create Database
# REDIS_URL=rediss://default:...@us1-xxx.upstash.io:6379

# -----------------------------------------------------------------------------
# Vercel-Specific Notes
# -----------------------------------------------------------------------------
# 1. Add all variables in: Vercel Dashboard → Settings → Environment Variables
# 2. Select "Production" environment (or all environments)
# 3. Redeploy after adding new variables
# 4. Never commit secrets to Git
# 5. Use different secrets for production vs development

# -----------------------------------------------------------------------------
# Environment Variables Checklist
# -----------------------------------------------------------------------------
# Required for basic functionality:
# [✓] DATABASE_URL
# [✓] APP_URL
# [✓] NODE_ENV
# [✓] SESSION_SECRET
# [✓] AI_PROVIDER
# [✓] [AI_PROVIDER]_API_KEY (e.g., OPENAI_API_KEY)
# [✓] [AI_PROVIDER]_MODEL (e.g., OPENAI_MODEL)
#
# Optional for OAuth:
# [ ] GOOGLE_CLIENT_ID
# [ ] GOOGLE_CLIENT_SECRET
#
# Optional for caching:
# [ ] REDIS_URL
